import React, {FunctionComponent} from 'react';

// const client = getSdk({request() {
//     console.log('===>', arguments);

// }} as any, (action, name, type) => {
//     console.log(action, name, type);

//     const res = action()
//     console.log(res);
// });

export const PageSettings:FunctionComponent<{}> = () => {
    // client.FindUser({search:''});4434

    return <>
        <h2>Settings</h2>
    </>;
}